route:
  receiver: default
  group_by: ['alertname', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    # Wire Slack/PagerDuty webhook in production:
    # webhook_configs:
    #   - url: 'https://hooks.slack.com/services/...'
