{
  "name": "daily-rashan-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:admin": "playwright test tests/admin-login.spec.ts",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
